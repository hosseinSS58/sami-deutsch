# 📝 راهنمای پر کردن فیلدهای Python App در cPanel

این راهنما به شما کمک می‌کند که فیلدهای Python App را در cPanel به درستی پر کنید.

---

## 🎯 فیلدهای Python App

### 1️⃣ **Python version**
**چی باید انتخاب کنید:**
- آخرین نسخه Python موجود (معمولاً Python 3.8, 3.9, 3.10, یا 3.11)
- **توصیه:** Python 3.10 یا بالاتر

**مثال:**
```
Python 3.10
```

---

### 2️⃣ **Application root**
**چی باید وارد کنید:**
مسیر فیزیکی پروژه روی سرور. این مسیر معمولاً به این شکل است:

```
/home/username/public_html
```

یا اگر دامنه جداگانه دارید:
```
/home/username/public_html/yourdomain.com
```

**نکات مهم:**
- `username` را با username cPanel خود جایگزین کنید
- این مسیر باید همان جایی باشد که فایل‌های پروژه را آپلود کرده‌اید
- معمولاً `public_html` است

**چطور پیدا کنید:**
1. در cPanel به **File Manager** بروید
2. به پوشه‌ای که `manage.py` و `passenger_wsgi.py` در آن است بروید
3. در بالای صفحه، مسیر کامل را می‌بینید (مثلاً `/home/samideut/public_html`)

**مثال:**
```
/home/samideut/public_html
```

---

### 3️⃣ **Application URL**
**چی باید وارد کنید:**
URL که می‌خواهید سایت روی آن اجرا شود.

**گزینه‌ها:**

**الف) اگر می‌خواهید سایت در root domain اجرا شود:**
```
/
```

**ب) اگر می‌خواهید در subdomain اجرا شود:**
```
/app
```
یا
```
/django
```

**مثال:**
```
/
```

**نکته:** اگر `/` انتخاب کنید، سایت در `yourdomain.com` اجرا می‌شود.

---

### 4️⃣ **Application startup file**
**چی باید وارد کنید:**
نام فایلی که Passenger از آن برای شروع اپلیکیشن استفاده می‌کند.

```
passenger_wsgi.py
```

**نکات مهم:**
- این فایل باید در **Application root** (همان مسیری که در فیلد 2 وارد کردید) باشد
- نام فایل باید دقیقاً `passenger_wsgi.py` باشد
- این فایل در پروژه شما موجود است

**مثال:**
```
passenger_wsgi.py
```

---

### 5️⃣ **Application Entry point**
**چی باید وارد کنید:**
نام متغیر WSGI که در فایل `passenger_wsgi.py` تعریف شده است.

```
application
```

**نکات مهم:**
- این متغیر در فایل `passenger_wsgi.py` به صورت `application = application` تعریف شده
- همیشه `application` است (بدون فاصله، با حروف کوچک)

**مثال:**
```
application
```

---

### 6️⃣ **Passenger log file**
**چی باید وارد کنید:**
مسیر فایل لاگ برای Passenger. این فایل خطاها و اطلاعات اجرا را ذخیره می‌کند.

**فرمت:**
```
/home/username/logs/passenger.log
```

**نکات مهم:**
- `username` را با username cPanel خود جایگزین کنید
- پوشه `logs` باید وجود داشته باشد (اگر نیست، از File Manager ایجاد کنید)
- نام فایل می‌تواند `passenger.log` یا هر نام دیگری باشد

**چطور ایجاد کنید:**
1. در cPanel به **File Manager** بروید
2. به پوشه `home` بروید
3. پوشه `logs` را ایجاد کنید (اگر وجود ندارد)
4. دسترسی‌ها را روی `755` تنظیم کنید

**مثال:**
```
/home/samideut/logs/passenger.log
```

---

## 📋 مثال کامل

فرض کنید:
- Username cPanel شما: `samideut`
- Domain: `samideutsch.com`
- می‌خواهید سایت در root domain اجرا شود

**مقادیر:**

| فیلد | مقدار |
|------|-------|
| **Python version** | `Python 3.10` |
| **Application root** | `/home/samideut/public_html` |
| **Application URL** | `/` |
| **Application startup file** | `passenger_wsgi.py` |
| **Application Entry point** | `application` |
| **Passenger log file** | `/home/samideut/logs/passenger.log` |

---

## ✅ چک‌لیست قبل از Create

قبل از اینکه روی **Create** کلیک کنید، مطمئن شوید:

- [ ] فایل‌های پروژه در **Application root** آپلود شده‌اند
- [ ] فایل `passenger_wsgi.py` در **Application root** موجود است
- [ ] فایل `manage.py` در **Application root** موجود است
- [ ] پوشه `logs` ایجاد شده (برای Passenger log file)
- [ ] Python version مناسب انتخاب شده (3.8+)

---

## 🔍 چطور مسیرها را پیدا کنید؟

### پیدا کردن Application root:

1. در cPanel به **File Manager** بروید
2. به پوشه‌ای که `manage.py` در آن است بروید
3. در بالای صفحه، مسیر کامل را می‌بینید

**مثال:**
```
Current Path: /home/samideut/public_html
```

### پیدا کردن Username:

1. در cPanel، در گوشه بالا راست، username خود را می‌بینید
2. یا در **File Manager**، مسیرها با `/home/username/` شروع می‌شوند

---

## 🐛 مشکلات رایج

### مشکل: "Application root not found"
**راه‌حل:**
- مطمئن شوید که مسیر درست است
- از File Manager مسیر را کپی کنید
- دقت کنید که `/` در ابتدای مسیر باشد

### مشکل: "Startup file not found"
**راه‌حل:**
- مطمئن شوید که `passenger_wsgi.py` در Application root است
- نام فایل باید دقیقاً `passenger_wsgi.py` باشد (با حروف کوچک)

### مشکل: "Entry point not found"
**راه‌حل:**
- مطمئن شوید که در فایل `passenger_wsgi.py` متغیر `application` تعریف شده
- مقدار باید دقیقاً `application` باشد (بدون فاصله)

---

## 📸 تصویر نمونه

```
┌─────────────────────────────────────────┐
│ Python App Setup                       │
├─────────────────────────────────────────┤
│ Python version:                         │
│ [Python 3.10 ▼]                         │
│                                         │
│ Application root:                       │
│ [/home/samideut/public_html]           │
│                                         │
│ Application URL:                        │
│ [/]                                     │
│                                         │
│ Application startup file:               │
│ [passenger_wsgi.py]                    │
│                                         │
│ Application Entry point:                │
│ [application]                           │
│                                         │
│ Passenger log file:                     │
│ [/home/samideut/logs/passenger.log]    │
│                                         │
│         [Create]  [Cancel]             │
└─────────────────────────────────────────┘
```

---

## 🚀 بعد از Create

بعد از اینکه Python App را ایجاد کردید:

1. **Virtual Environment** به صورت خودکار ایجاد می‌شود
2. مسیر virtual environment معمولاً:
   ```
   /home/username/virtualenv/public_html/3.10/
   ```
3. حالا باید dependencies را نصب کنید (مرحله بعدی در راهنما)

---

**موفق باشید! 🎉**

اگر سوالی دارید یا مشکلی پیش آمد، بپرسید.







