# 📺 راهنمای وارد کردن ویدیو از یوتیوب

این راهنما نحوه استفاده از سیستم وارد کردن خودکار ویدیوهای یوتیوب را توضیح می‌دهد.

## ✨ ویژگی‌ها

- ✅ استخراج خودکار اطلاعات از لینک یوتیوب
- ✅ پیش‌نمایش اطلاعات قبل از ایجاد
- ✅ ایجاد خودکار ویدیو و لینک یوتیوب
- ✅ پشتیبانی از فرمت‌های مختلف لینک یوتیوب
- ✅ رابط کاربری ساده در پنل ادمین

## 🚀 نحوه استفاده

### روش 1: از صفحه لیست ویدیوها

1. به پنل ادمین بروید: `/admin/courses/video/`
2. روی دکمه **"وارد کردن از یوتیوب"** در بالای صفحه کلیک کنید
3. لینک ویدیو یوتیوب را وارد کنید
4. روی دکمه **"استخراج اطلاعات"** کلیک کنید
5. اطلاعات استخراج شده را بررسی کنید
6. تنظیمات ویدیو را تعیین کنید (سطح، موضوع، دشواری، و ...)
7. روی **"ایجاد ویدیو"** کلیک کنید

### روش 2: از URL مستقیم

می‌توانید مستقیماً به آدرس زیر بروید:
```
/admin/courses/video/import-youtube/
```

## 📋 فرمت‌های لینک یوتیوب پشتیبانی شده

سیستم از فرمت‌های زیر پشتیبانی می‌کند:

- `https://www.youtube.com/watch?v=VIDEO_ID`
- `https://youtu.be/VIDEO_ID`
- `https://www.youtube.com/embed/VIDEO_ID`
- `https://www.youtube.com/shorts/VIDEO_ID`

## 🔍 اطلاعات استخراج شده

سیستم به طور خودکار این اطلاعات را از ویدیو یوتیوب استخراج می‌کند:

- عنوان ویدیو
- توضیحات ویدیو
- مدت زمان (به دقیقه)
- تصویر کاور (thumbnail)
- نام کانال
- شناسه کانال
- تعداد بازدید
- تعداد لایک
- تاریخ آپلود
- تگ‌ها و دسته‌بندی‌ها

## ⚙️ تنظیمات قابل تغییر

پس از استخراج اطلاعات، می‌توانید این تنظیمات را تغییر دهید:

- **سطح**: A1, A2, B1, B2, C1
- **موضوع**: گرامر، مکالمه، واژگان، شنیداری، خواندن، نوشتن، فرهنگ، تلفظ
- **سطح دشواری**: مبتدی، متوسط، پیشرفته
- **رایگان/پولی**: تعیین اینکه ویدیو رایگان است یا پولی
- **ویژه**: آیا ویدیو باید به عنوان ویدیو ویژه نمایش داده شود

## 🛠️ فایل‌های ایجاد شده

### 1. `courses/youtube_utils.py`
ابزارهای کمکی برای استخراج اطلاعات از یوتیوب:
- `extract_youtube_id()`: استخراج ID ویدیو از لینک
- `extract_youtube_metadata()`: استخراج کامل اطلاعات ویدیو
- `create_video_from_youtube()`: ایجاد Video و YouTubeLink از لینک

### 2. `courses/admin.py`
- صفحه ادمین برای وارد کردن ویدیو
- API endpoint برای استخراج AJAX اطلاعات
- دکمه در صفحه لیست ویدیوها

### 3. `templates/admin/courses/video/import_youtube.html`
صفحه وارد کردن ویدیو با:
- فرم وارد کردن لینک
- پیش‌نمایش اطلاعات استخراج شده
- تنظیمات ویدیو
- رابط کاربری AJAX

### 4. `templates/admin/courses/video/change_list.html`
افزودن دکمه "وارد کردن از یوتیوب" به صفحه لیست

## 📝 نکات مهم

1. **yt-dlp**: برای استخراج اطلاعات از yt-dlp استفاده می‌شود که باید در `requirements.txt` موجود باشد ✅

2. **Slug خودکار**: slug به طور خودکار از عنوان ویدیو ایجاد می‌شود و در صورت تکرار، شماره اضافه می‌شود

3. **محدودیت طول**: 
   - عنوان: حداکثر 200 کاراکتر
   - Slug: حداکثر 220 کاراکتر
   - توضیحات Video: تا 2000 کاراکتر نگه داشته می‌شود
   - توضیحات YouTubeLink: حداکثر 1000 کاراکتر

4. **مدیریت خطا**: در صورت خطا در استخراج، پیام خطا نمایش داده می‌شود

## 🔄 نمونه استفاده برنامه‌نویسی

اگر می‌خواهید از طریق کد Python استفاده کنید:

```python
from courses.youtube_utils import create_video_from_youtube

# ایجاد ویدیو از لینک یوتیوب
video, youtube_link, message = create_video_from_youtube(
    "https://www.youtube.com/watch?v=VIDEO_ID",
    level="A2",
    topic="grammar",
    difficulty="intermediate",
    is_free=True,
    is_featured=False,
)

if video:
    print(f"ویدیو ایجاد شد: {video.title}")
else:
    print(f"خطا: {message}")
```

## 🐛 عیب‌یابی

### مشکل: "خطا در دریافت اطلاعات"

- بررسی کنید که لینک یوتیوب معتبر باشد
- اطمینان حاصل کنید که ویدیو در دسترس است (خصوصی یا حذف نشده باشد)
- بررسی کنید که yt-dlp به‌روز است: `pip install --upgrade yt-dlp`

### مشکل: "لینک یوتیوب معتبر نیست"

- مطمئن شوید فرمت لینک صحیح است
- از یکی از فرمت‌های پشتیبانی شده استفاده کنید

## 📚 منابع بیشتر

- [مستندات yt-dlp](https://github.com/yt-dlp/yt-dlp)
- [مستندات Django Admin](https://docs.djangoproject.com/en/stable/ref/contrib/admin/)










